public void defragging(Integer[] memory)
{
    // code here
    for(Integer mem : memory){
        if(mem < 0){
            memory.add(mem,memory.size()-1);
            memory.remove(indexOf(mem));
        }
    }
}