public void plusOne(List<Integer> numbers) {
    // code here
 Double joinedValue = Double.valueOf(String.join(numbers,''));
    joinedValue++;
    numbers.clear(); 
    for(String s : String.valueOf(joinedValue).split('')){
        numbers.add(Integer.valueOf(s));
    }
} 
