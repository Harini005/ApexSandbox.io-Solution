public Id insertStudent(String name, String email) {
    // code here
    apxio_Student__c record = new apxio_Student__c();
    record.Name = name;
    record.apxio__Email__c = email;
    insert record;
    return record.Id;
}